language: java
sudo: required
dist: trusty

matrix:
  include:
    - jdk: oraclejdk9 # JDK 9-ea+174 provided by Travis will be replaced by 9+177 in "before_install"
      before_install:
        - cd ~
        - wget http://download.java.net/java/jdk9/archive/178/binaries/jdk-9+178_linux-x86_bin.tar.gz
        - tar -xzf jdk-9+178_linux-x64_bin.tar.gz
        - export JAVA_HOME=~/jdk-9
        - PATH=$JAVA_HOME/bin:$PATH
        - cd -